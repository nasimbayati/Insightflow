# Competition Assets

## Recommended Submission Title

`InsightFlow: Decision-Ready Answers from Messy CSVs`

## Backup Title Options

- `InsightFlow: Turn Unpredictable CSVs into Executive Insights`
- `InsightFlow: Upload Any CSV, Get Clean Data and Clear Decisions`
- `InsightFlow: A Production-Style Pipeline for Messy CSV Analytics`

## One-Line Pitch

`A production-style CSV analytics app that validates messy files, cleans issues, and turns them into decision-ready insights.`

## 500-Character Submission Description

`InsightFlow turns messy CSVs into a decision brief instead of a raw table dump. I built it so students, analysts, and teams can upload unpredictable files and quickly understand quality risks, key drivers, and next actions. Built with Codex as a Streamlit pipeline with ingestion checks, schema validation, configurable cleaning, role-aware charts, rule-based insights, and downloadable reports.`

Character count target:

- 397 characters

## Recommended Demo Dataset

- [sample_revenue_ops_showcase.csv](/E:/Projects/InsightFlow/data/sample_revenue_ops_showcase.csv)

Why use it:

- visible data-quality problems
- a clear underperforming segment
- an obvious outlier
- enough structure for trend, distribution, category, and relationship charts

## 60-Second Demo Script

1. `Open the app. The bundled Revenue Operations Showcase is loaded by default.`
2. `Point to the Boardroom Brief and read the quality score, insight confidence, top risk, key driver, and first recommended action.`
3. `Show the Column Roles controls and explain that InsightFlow can treat fields as IDs, time axes, metrics, segments, or outcomes instead of hardcoding demo columns.`
4. `Open Data Validation and show the missing values, duplicate records, and invalid numeric/date entries.`
5. `Point to Cleaning Impact and mention that the pipeline uses configurable cleaning rather than a hardcoded policy.`
6. `Open Guided Exploration, show the Chart Builder, and choose the exact grouping and metric for a chart.`
7. `Use the analysis package download as proof that the app creates handoff-ready assets.`
8. `Close with: InsightFlow took a messy CSV and turned it into a decision-ready explanation in under a minute, without requiring API access.`

## Screenshot Plan

Use these 4 screenshots in this order:

1. `Hero + Choose Data Source`
   - show the branded top section and bundled demo option
2. `Decision Mode`
   - include quality score, confidence, top risk, key driver, and one evidence chart
3. `Column Roles + Current Interpretation`
   - show the generic role controls and assumptions bar
4. `Data Validation`
   - show missing values, duplicate preview, and invalid numeric/date checks
5. `AI Insights + Recommended Charts`
   - show the insight cards and chart explanations together

## Final Submission Checklist

- use the recommended title and one-line pitch
- use the 500-character description with the project URL
- record the 45 to 60 second demo on the bundled showcase dataset
- capture the 4 screenshots listed above
- mention that the app works without API access and that LLM mode is optional
- check the `Built with Codex` box

## Short Judge Takeaway

`This is not just a CSV viewer. It is a reliable analytics workflow for unpredictable business files.`
